import { Dimensions, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title:{
        fontWeight:'bold',
        marginVertical:10,
        fontSize:18,
        color:'black',
        paddingHorizontal:5

    },
    text:{
        color:'black',
        fontSize:14,
        marginBottom:10,
        paddingHorizontal:5
    }
 
});

export default styles;